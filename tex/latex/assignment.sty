\usepackage{etoolbox}
\usepackage{environ}
\usepackage{comment}
\usepackage[margin=1in]{geometry}


\newenvironment{question}
{\item}{}

\newenvironment{questions}
{\begin{enumerate}}
{\end{enumerate}}

\newenvironment{qs}
{\begin{enumerate}}{\end{enumerate}}

%use the two lines to turn on (resp. off) solutions (use Ultisnips "ass" to get the appropriate order automatically in the preamble)
%\showtrue
%\showfalse

\ifshow
    \newenvironment{solution}
    {\begin{sol} \color{blue}}
    {\end{sol}}
\else
    \excludecomment{solution}
\fi
