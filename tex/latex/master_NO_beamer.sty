% these packages are incompatible with beamer so I had to move it
\usepackage{enumitem}
\usepackage{moreenum}

% beamer is incompatible with backref
% I call hyperref (with backref) in master.sty for usual documents
% I call hyperref in the preamble of the "beam" ultisnip instead.
% ---
% hypertexnames is roughly for "things being numbered within other things" 
% hidelinks hides the ugly borders
% backref puts the back references in the references section at the end
% alphabetic labels references with the authors' last names and year 
% ----
% If you don't want to use amsrefs put:
% \bibliographystyle{amsalpha}
% before \bibliography{source} at the end
% ---
% \usepackage[backrefs,shortalphabetic]{amsrefs}
\usepackage[nameinlink]{cleveref}
% \usepackage[nameinlink,capitalise]{cleveref}
\usepackage{nameref}
%aesthetic setup for links
\hypersetup{colorlinks = true,
            linkcolor = blue,
            % linkbordercolor=red,
            urlcolor  = blue,
            citecolor = black,
}
% uncomment the following to have colored text and outlines (which look like underlines in Adobe)
% \makeatletter
% \Hy@AtBeginDocument{%
%   \def\@pdfborder{0 0 1}% Overrides border definition set with colorlinks=true
%   \def\@pdfborderstyle{/S/U/W 1}% Overrides border style set with colorlinks=true
%                                 % Hyperlink border style will be underline of width 1pt
% }
% \makeatother

% beamer also hates some of my environments
%ENVIRONMENTS
%environments get:
%	definition
%	crefname (theorems, lemmas, propositions, and conjectures are always capitalized.
%	Everything else is controlled by cref)
%	numberwithin setting(in cls files) (e.g. 
%\numberwithin{equation}{chapter})
%	Snippet

\theoremstyle{plain}
\newtheorem{fact}{Fact}
\Crefname{fact}{Fact}{Facts}
\newtheorem*{fact*}{Fact}
%
\newtheorem{note}{Note}
\Crefname{note}{Note}{Notes}
\newtheorem*{note*}{Note}
%
\newenvironment{Proof}
    {\begin{proof}}
    {\renewcommand{\qedsymbol}{$\blacksquare$}
    \end{proof}
    \renewcommand{\qedsymbol}{$\square$}}
